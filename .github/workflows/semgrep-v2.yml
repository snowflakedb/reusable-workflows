name: Reusable workflow for PRs

on:
  workflow_call:
   secrets:
     token:
       required: true

permissions:
  contents: read
 
jobs:
  semgrep:
    name: Scan
    runs-on: ubuntu-22.04
    steps:
      - name: "No-op"
        run: echo "Temporarily disabled due to Docker outage"
      
